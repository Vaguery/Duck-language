#encoding:utf-8
require_relative './spec_helper'

describe "Closures have an :injectable? method" do
  it "should refer to the Closure's returned_items_responses attribute"
  
  it "should check to see if the return class satisfies one of its own needs"
  
  it "should be true for Closures that return Arrays of results, if one item in those responds to a need"
end